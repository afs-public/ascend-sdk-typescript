# IdentityLookupType

The type of identification document

## Example Usage

```typescript
import { IdentityLookupType } from "@apexfintechsolutions/ascend-sdk/models/components";

let value: IdentityLookupType = IdentityLookupType.Ssn;
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

| Name                   | Value                  |
| ---------------------- | ---------------------- |
| `TypeUnspecified`      | TYPE_UNSPECIFIED       |
| `Ssn`                  | SSN                    |
| -                      | `Unrecognized<string>` |